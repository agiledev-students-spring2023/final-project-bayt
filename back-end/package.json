{
  "name": "back-end",
  "version": "1.0.0",
  "description": "The back-end of your project will live in this directory.",
  "main": "server.js",
  "scripts": {
    "start": "npx nodemon server",
    "test": "npx mocha --recursive",
    "coverage": "npx nyc --all --include '*/**' --exclude '**/test/**' --reporter lcov --reporter text npm run test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "chai": "^4.3.7",
    "chai-http": "^4.3.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "istanbul": "^0.4.5"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "nodemon": "^2.0.21",
    "nyc": "^15.1.0"
  }
}
